spring:
  application:
    name: image-clustering-module-test

  config:
    activate:
      on-profile: test

  datasource:
    url: jdbc:${DB_BASE}://${DB_HOST}:${DB_PORT}/${DB_NAME}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}