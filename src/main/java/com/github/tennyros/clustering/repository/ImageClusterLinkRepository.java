package com.github.tennyros.clustering.repository;

import com.github.tennyros.clustering.entity.ImageClusterLink;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ImageClusterLinkRepository extends JpaRepository<ImageClusterLink, Long> {
}
